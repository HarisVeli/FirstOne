#steps

susikuriam failus
git init
git add .
git commit -m 'init'
susikurti repozitorija githube
susieti sita lokasu projekta su github repozitorija (3eilutes is github) "push an existing repository from the command line"


#dirbam

git add .
git commit -m 'zinute'
git push